{
  "name": "benchmaster",
  "private": true,
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.8.2"
  },
  "volta": {
    "node": "20.19.0",
    "npm": "10.8.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "check:node": "node scripts/check-node.mjs",
    "preinstall": "node scripts/check-node.mjs",
    "install:all": "npm run check:node && npm install --workspaces",
    "dev:frontend": "npm run check:node && npm run start --workspace frontend",
    "dev:backend": "npm run check:node && npm run dev --workspace backend",
    "build": "npm run check:node && npm run build --workspace backend && npm run build --workspace frontend",
    "db:migrate": "npm run check:node && npm run prisma:migrate --workspace backend",
    "db:setup": "npm run check:node && npm run prisma:generate --workspace backend && npm run db:setup --workspace backend",
    "db:seed": "npm run check:node && npm run db:seed --workspace backend"
  }
}
